services:
  - type: web
    name: colab-command-center
    plan: starter
    region: oregon
    healthCheckPath: /api/healthz
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000